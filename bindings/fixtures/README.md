# Binding compatibility fixtures

`v1-basic` and `v2-basic` are deterministic compiled-directory fixtures made
from `tests/fixtures/basic_vault` by the frozen V1 and V2 compilers. They are
read-only test inputs for the Python and Node.js auto-detection, verification,
and provenance-explanation APIs. Do not regenerate them with the V3 writer.
